#ifndef PAZAAK_EVENTS_HPP
#define PAZAAK_EVENTS_HPP

// \todo this could merge with engine.hpp event enum class, but should starts after the last position of, modify this!

enum class Event {
  ShowSoloView = 3,
  ShowMultiView,
  ShowSettingsView
}

#endif // PAZAAK_EVENTS_HPP
