#ifndef DEFINES_HPP
#define DEFINES_HPP

#include <string>

const std::string TEXTURE_PATH {"resources/images/"};

struct Size
{
  uint width;
  uint height;
};

struct Position
{
  int x;
  int y;
};

#endif // DEFINES_HPP
